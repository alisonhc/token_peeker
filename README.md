# Token Peeker :eyes:

Welcome to **Token Peeker**:eyes:, a toolbox for Visualization & Analysis of LM Token Distributions!

## Setup

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```